#順次処理
# | で処理を連結（パイプ）することができる。
# % は ForEach-Object のエイリアスで、渡された配列を順次処理する。
# {echo %_} は、 ForEach-Object の第一引数（-Process）で、スクリプトブロックと呼ばれる。
#詳しくは、 help ForEach-Object を参照。
# $_ は、パイプで受け取ったオブジェクトを参照するための暗黙の変数。
@(1..5) | %{echo $_}